<?php
/**
 * B2B Register Button Widget Template
 * @var \GrupoAwamotos\B2B\Block\Widget\RegisterButton $block
 */
?>
<?php if ($block->shouldDisplay()): ?>
<div class="b2b-register-widget">
    <a href="<?= $block->escapeUrl($block->getRegisterUrl()) ?>" 
       class="action <?= $block->escapeHtmlAttr($block->getButtonStyle()) ?>"
       title="<?= $block->escapeHtmlAttr($block->getButtonText()) ?>">
        <span class="b2b-icon">🏢</span>
        <span><?= $block->escapeHtml($block->getButtonText()) ?></span>
    </a>
</div>

<style>
.b2b-register-widget {
    display: inline-block;
}

.b2b-register-widget .action {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    border-radius: 6px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
}

.b2b-register-widget .b2b-btn-primary {
    background: linear-gradient(90deg, #f39c12, #e67e22);
    color: #fff;
    border: none;
}

.b2b-register-widget .b2b-btn-primary:hover {
    background: linear-gradient(90deg, #e67e22, #d35400);
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(243, 156, 18, 0.4);
}

.b2b-register-widget .b2b-btn-secondary {
    background: linear-gradient(90deg, #3498db, #2980b9);
    color: #fff;
    border: none;
}

.b2b-register-widget .b2b-btn-secondary:hover {
    background: linear-gradient(90deg, #2980b9, #1f5f89);
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(52, 152, 219, 0.4);
}

.b2b-register-widget .b2b-icon {
    font-size: 18px;
}
</style>
<?php endif; ?>
