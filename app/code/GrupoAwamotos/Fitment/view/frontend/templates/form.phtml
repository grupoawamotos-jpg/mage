<div class="fitment-search" data-fitment-form>
    <form action="<?= $block->getUrl('fitment/fitment/index') ?>" method="get" class="fitment-form" data-role="fitment-form">
        <fieldset>
            <legend><?= __('Buscar Peças por Moto') ?></legend>
            <div class="fields">
                <label class="field">
                    <span class="label"><?= __('Marca') ?></span>
                    <select name="marca_moto" id="fitment-marca" data-fitment="marca">
                        <option value=""><?= __('Selecione a Marca') ?></option>
                        <?php foreach($block->getDistinctValues('marca_moto') as $marca): ?>
                            <option value="<?= $block->escapeHtml($marca) ?>"><?= $block->escapeHtml($marca) ?></option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <label class="field">
                    <span class="label"><?= __('Modelo') ?></span>
                    <select name="modelo_moto" id="fitment-modelo" data-fitment="modelo" disabled>
                        <option value=""><?= __('Selecione a Marca primeiro') ?></option>
                    </select>
                </label>
                <label class="field">
                    <span class="label"><?= __('Ano') ?></span>
                    <select name="ano_moto" id="fitment-ano" data-fitment="ano" disabled>
                        <option value=""><?= __('Selecione o Modelo primeiro') ?></option>
                    </select>
                </label>
            </div>
            <div class="actions">
                <button type="submit" class="action primary" disabled id="fitment-submit"><span><?= __('Buscar') ?></span></button>
            </div>
        </fieldset>
    </form>
</div>
<script type="text/x-magento-init">
{
    "[data-fitment-form]": {
        "GrupoAwamotos_Fitment/js/fitment": {
            "endpoints": {
                "models": "<?= $block->getUrl('fitment/ajax/models') ?>",
                "years": "<?= $block->getUrl('fitment/ajax/years') ?>"
            }
        }
    }
}
</script>
